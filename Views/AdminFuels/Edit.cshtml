@model Carzi.Models.FuelType

@{
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
}

<h3>Edit Fuel</h3>

<form method="post">
    <div class="mb-3">
        <label>Name</label>
        <input class="form-control" name="name" value="@Model.Name" required />
    </div>
    
    <div class="mb-3">
        <label>Price (€ / liter)</label>
        <input type="number" step="0.01" class="form-control" name="pricePerLiter" value="@Model.PricePerLiter" required />
    </div>

    <br>
    <button class="btn btn-primary">Update</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>
